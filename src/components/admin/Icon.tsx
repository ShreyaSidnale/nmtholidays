import React from 'react'

/* Small nav-header brand icon for the Payload admin (visual only). */
export const Icon = () => (
  // eslint-disable-next-line @next/next/no-img-element
  <img src="/nmt-logo.png" alt="NMT" style={{ height: 26, width: 'auto' }} />
)

export default Icon
